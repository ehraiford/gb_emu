{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceRoot}/target/debug/gb_emu.exe",
            "args": [
                "run-as-fast-as-possible",
                "${workspaceFolder}/test_roms/blargg_test_roms/11-op a,(hl).gb"
            ],
            "cwd": "${workspaceRoot}"
        }
    ]
}